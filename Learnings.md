1. Git is a version control system that tracks changes made to a codebase over time. It allows developers to collaborate on the same project, maintain a history of changes, revert to previous working versions if something breaks, and manage contributions from multiple team members efficiently. GitHub is an online hosting platform for Git repositories.
2. Git is a version control system that records changes to code over time. Unlike storing multiple folders, Git tracks the exact differences between versions, records who made each change and why, allows developers to collaborate without overwriting each other's work, and makes it easy to revert to previous working versions if bugs are introduced.
3. Node.js lets me run JavaScript outside the browser.
4. React is a library and next.js is a framework built on top of react
5. Some commands : pwd - print working directory where u are basically. ls-list all files in the folder. git init - Think of it as telling Git, "Start watching this folder and track all the changes I make from now on." When you run it, Git creates a hidden .git folder inside your project directory, which is where it stores all the tracking data and version history.
6. git remote add origin https://github.com/D3VYXNSH/wanderlust-ai.git - "Git, please remember that my GitHub repository lives at this address, and whenever I say origin, I mean that repository."
7. -v means verbose (show me more details)
8. push - You are sending your work to GitHub. fetch - "Has anything changed? Give me the latest information."
9. git add . - add everything to current folder
10. -m means message . git commit -m "Initial project setup"
11. git push -u origin main ( -u. means upload to main. Now after refreshing githubs our files will be online. )
12. npm - node project manager is the app store for everything java script like libraries frameworks etc
13. HTML -> JS -> React (build interface using components) -> next.js ( to add routing perfomance API routes, easier deployment)
14. for commands info and how we set up our project and local host refer to notes.
15. To avoid our page.tsx file becoming extremely long we will make components for different features and page.tsx will just connect them. So component is basically a reuasable piece of a webpage. 
16. All changes u want to see on http://localhost:3000 just press command + s then see. Also press ctrl + c to close the local host server once done working